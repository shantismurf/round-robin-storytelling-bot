{
  "name": "round-robin-storybot",
  "version": "1.2.0",
  "description": "A Discord bot to run round-robin collaborative stories",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "deploy": "node deploy.js",
    "start": "node index.js",
    "sync-config": "node sync-config.js",
    "sync-config:apply": "node sync-config.js --apply",
    "sync-config:set": "node sync-config.js --set",
    "dev": "nodemon index.js",
    "lint": "eslint . || true",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "discord.js": "^14.25.1",
    "marked": "^17.0.4",
    "mysql2": "^3.15.3"
  }
}
